# Back-End API

Enjoy my endpoints 

### Admin Endpoints

- `GET` : `api/admin/users`
- `GET` : `api/admin/circuits`
- `GET` : `api/admin/circuits/:id`

### Api Endpoints

- `GET` : `api/me`

### Public Endpoints

- `POST` : `pub/login` | {email, password}
